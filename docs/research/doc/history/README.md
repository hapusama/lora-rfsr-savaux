# History Archive

This folder keeps Markdown notes moved out of retired weak-decoder experiment
trees.  The original relative paths are preserved under this directory so old
handoff notes and reports remain discoverable without keeping obsolete Python
packages alive.

Archived sources include:

- `scripts/experiments/phase_line/`
- `weak_decoder/phase_line/`
- `weak_decoder/pure_phaseline/`
- `weak_decoder/v1/`, `weak_decoder/v2/`, `weak_decoder/v3/`

