# License map

This repository is a combined research workspace and contains code with
different provenance.

- The repository integration code and the code migrated from
  `gr-lora_sdr/weakPacket_decoding` are distributed under GPL-3.0-only. The
  full text is available in the repository root `LICENSE` and in
  `LICENSES/gr-lora_sdr-GPL-3.0.txt`.
- `third_party/rfsr` retains the upstream RFSuperResolution license in
  `third_party/rfsr/LICENSE`.
- The upstream RFSuperResolution README states that `rfsr/PHY.py` is based on
  SDR-LoRa and is excluded from the upstream MIT license. Do not assume that
  the MIT grant covers that file.

Retain the corresponding notices when redistributing either component.
